{
  "mcpServers": {
    "opusclip": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server.mjs"],
      "env": {
        "OPUSCLIP_API_KEY": "${OPUSCLIP_API_KEY}"
      }
    }
  }
}
